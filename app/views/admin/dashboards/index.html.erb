<h1>Listing dashboards</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @dashboards.each do |dashboard| %>
      <tr>
        <td><%= link_to 'Show', [:admin, dashboard] %></td>
        <td><%= link_to 'Edit', edit_admin_dashboard_path(dashboard) %></td>
        <td><%= link_to 'Destroy', [:admin, dashboard], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Dashboard', new_admin_dashboard_path %>
